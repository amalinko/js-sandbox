{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tsc check",
            "type": "shell",
            "command": "bunx tsc --noEmit --strict ${file} --ignoreConfig",
            "problemMatcher": "$tsc"
        }
    ]
}