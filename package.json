{
  "name": "ts-browser-app",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun run dev.ts",
    "build": "bun build src/main.ts --outdir dist --minify --sourcemap=external --target=browser"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
